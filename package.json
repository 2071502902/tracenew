{
  "name": "trace-note",
  "version": "1.0.0",
  "private": true,
  "description": "Trace Note Capacitor Android wrapper",
  "scripts": {
    "cap:add:android": "cap add android",
    "cap:sync:android": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  }
}
